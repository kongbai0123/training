[pytest]
testpaths = tests
norecursedirs = build dist release_artifacts .git __pycache__
addopts = --strict-markers
markers =
    gpu: requires a CUDA-capable device; never selected on CI runners
    heavy: requires the deep-learning stack (torch, torchvision, opencv, pillow) but runs on CPU
