# Pure-Python build packages that must match the already published runtime-r1.
#
# Use only when building an application-only incremental update against
# updates/baselines/runtime-r1.json. Install normal runtime/build requirements
# first, then apply these versions without dependency resolution:
#
#   python -m pip install --force-reinstall --no-deps \
#     -r updates/baselines/runtime-r1-build-overrides.txt
#
# A full installer may adopt newer packages only with a new runtime baseline.
click==8.3.1
fastapi==0.135.1
filelock==3.20.0
markupsafe==3.0.2
packaging==26.0
pillow==12.0.0
protobuf==5.29.6
pydantic==2.12.5
pydantic-core==2.41.5
pyyaml==6.0.3
regex==2026.2.28
requests==2.32.5
sse-starlette==0.10.3
starlette==0.52.1
tqdm==4.67.3
ultralytics==8.4.22
uvicorn==0.42.0
websockets==16.0
